# 最终交付栏目模板

下面是 `style_mode=direct` 默认输出的栏目顺序与每个栏目的写作规约。**bazi / astrology 两个分支共用同一套骨架**，只在标 `[分支差异]` 的地方按方法处理。

> **合盘（synastry）分支有自己的独立输出结构**，由 `prompt/synastry.md` 定义——不套用本模板。本模板只覆盖单人解读。

---

## 1. 资料确认

**必出**。一段短陈述：

- 姓名 + 性别
- 出生：公历 → 农历对照（八字分支）或纯公历（星座分支）、时辰
- 出生地：城市 + 经纬度（如已知）+ 时区
- **[分支差异]**：
  - bazi 分支：列引擎算出的四柱（一行）+ 日主天干（一行）
  - astrology 分支：列太阳/月亮/上升/天顶四要素（一行）+ 使用的宫制 / zodiac type
- 真太阳时是否修正（默认未修正）

格式建议：用 markdown 列表，5-7 行内结束。

---

## 2. 结论

**必出**。3-5 句话总论：

- 一句话点出**格局核心**（如"印重身强、食神生财、杀印相生"等）
- 一句话点出**主要矛盾**（哪个五行过/缺、忌神在哪）
- 一句话点出**当前大运的主旋律**
- 必要时一句话点出**外象与内核的反差**（如"外在直接、内在思虑重"）

不要写成段落散文。每句信息密度要高。

---

## 3. 事业

**必出**。结构：

- **适配赛道**：列 3-5 类工作 / 行业方向
- **核心逻辑**：从月柱 / 喜用神 / 大运推导出来的"为什么是这些方向"
- **关键窗口**：当前/接下来 10 年大运的事业主题
- **不适配方向**：明确写出来什么类型的环境会消耗他/她

---

## 4. 财运

**必出**。结构：

- **财运模式**：稳健积累型 / 进取型 / 流动型 / 财不显型 之一
- **有利方向**：与喜用神一致的财路
- **风险点**：哪种钱不能赚、哪种合作要避开
- **大运财位**：当前大运里财星状态

---

## 5. 感情

**必出**。结构：

- **情感模式**：从配偶星（男看财、女看官杀）+ 月柱 + 桃花 + 星盘月亮金星综合
- **婚恋节奏**：早动 / 晚动 / 主动 / 被动
- **配偶倾向**：什么类型的人易匹配
- **风险点**：什么情境下关系容易出问题

---

## 6. 五年断事

**[分支差异]**：

### bazi 分支：必出

按 `year_window` 默认输出当年前两年 + 当年 + 后两年共 5 年。

每年格式：

```
**YYYY 干支｜十神主象**
一定发生：<基于流年干支 + 大运 + 命局可辩护的断事>
可能发生：<次概率事件，必要时给出风险点>
```

- "一定发生"要写**实际可执行的事件类别**（工作扩展、签约、晋升、搬家、健康警示、关系变动等），不要写形而上的玄学描述
- `assertiveness=high` 时允许使用强断句式；`medium` 用"倾向、容易"
- 不要编造精确事件细节（如"会遇到姓张的人"），保持事件**类别级**预测

### astrology 分支：改为"五年关键议题"

当前 engine 不计算 transit / progression，所以不做"哪一年一定 / 可能"的精确推运。改为输出**议题级**陈述：

- 外行星过境的大致议题（Saturn / Pluto 在本命盘上接近的位置）
- 当下 progression 阶段（如月相 / 太阳推运经过哪个本命星座）

如果无法负责任地输出（用户没有补充能用的 transit 资料），标注一句"此分支暂不做五年精确推运，仅做议题级提示"，然后给 2-4 条议题。

---

## 7. 建议

**必出**。3-6 条可执行建议，每条 1-2 句话。覆盖：

- 职业 / 第二曲线
- 财务纪律
- 健康（重点抓五行最缺的对应器官）
- 关系（婚恋节奏建议）
- 关键年的策略（针对五年里风险最大的那一年）

避免"多读书、保持积极心态"这种空话。

---

## 8. 尾注（默认，按分支）

bazi 分支：
```
*此为基于八字综合分析的玄学倾向性陈述，不作任何医疗、法律、投资保证。*
```

astrology 分支：
```
*此为基于西方占星本命盘的玄学倾向性陈述，不作任何医疗、法律、投资保证。*
```

---

## 渲染细则

- 默认中文。用户用英文提问时跟随英文。
- 不要在最终结果里写"用了什么方法 / 引擎输出 / facts JSON / rules 查表"。
- 不要写"六法速览 / 共同结论 / 分歧与置信度"等过程性栏目。
- 不要主动加 emoji。
- 关键术语第一次出现时可以白话解释一次，后续直接用术语。
- **跨分支术语隔离**：bazi 结果里不写'上升 / 太阳星座 / 金星'，astrology 结果里不写'日主 / 大运 / 十神 / 喜用神'。用户选了哪条就只用那条的术语系统。
